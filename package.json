{
  "name": "grt-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sudeepb02/grt-token",
    "create-local": "graph create --node http://localhost:8020/ sudeepb02/grt-token",
    "remove-local": "graph remove --node http://localhost:8020/ sudeepb02/grt-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sudeepb02/grt-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.1",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
